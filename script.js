// Funcție pentru a deschide un overlay specific
function openNav(id) {
    document.getElementById(id).style.height = "100%";
}

// Funcție pentru a închide un overlay specific
function closeNav(id) {
    document.getElementById(id).style.height = "0%";
}